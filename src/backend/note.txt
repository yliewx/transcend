To verify registration run the following commands in bash:
sqlite3 database.sqlite 
SELECT * FROM users;


To list tables:
.tables


To verify data inside the docker volume, run:
docker exec -it backend bash
sqlite3 /db/database.sqlite
// make sure it's the sqlite file in /db, instead of the original one in the backend directory