import { join } from 'path';

// constants.ts
export const publicDir = '/usr/src/app/public';
export const uploadsDir = join(publicDir, 'uploads');
export const avatarsDir = join(uploadsDir, 'avatars');
